{% extends '@PrestaShop/Admin/layout.html.twig' %}

{% block content %}

{# ------------------------------------------------------------------ #}
{# Flash messages                                                       #}
{# ------------------------------------------------------------------ #}
{% for label, messages in app.flashes %}
    {% for message in messages %}
        <div class="alert alert-{% if label == 'success' %}success{% elseif label == 'error' %}danger{% else %}info{% endif %} alert-dismissible fade show" role="alert">
            {{ message }}
            <button type="button" class="close" data-dismiss="alert" aria-label="Close">
                <span aria-hidden="true">&times;</span>
            </button>
        </div>
    {% endfor %}
{% endfor %}

{# ------------------------------------------------------------------ #}
{# Top action bar                                                       #}
{# ------------------------------------------------------------------ #}
<div class="d-flex align-items-center mb-3" style="gap:.5rem">
    <button type="button" class="btn btn-outline-secondary btn-sm" id="btn-toggle-config">
        <i class="material-icons" style="font-size:1rem;vertical-align:middle">settings</i>
        Paramètres
    </button>
    {% if logsPath is not empty %}
    <button type="button" class="btn btn-primary btn-sm tab-action tab-action-logs" id="btn-analyse">
        <i class="material-icons" style="font-size:1rem;vertical-align:middle">refresh</i>
        Actualiser
    </button>
    <div id="analyse-spinner" class="tab-action tab-action-logs" style="display:none">
        <div class="spinner-border spinner-border-sm text-primary" role="status">
            <span class="sr-only">Analyse en cours...</span>
        </div>
        <span class="ml-1 text-muted small">Analyse en cours...</span>
    </div>
    {% endif %}
    {% if cronUrl is not empty %}
    <button type="button" class="btn btn-outline-primary btn-sm tab-action tab-action-trafic tab-action-conversion" id="btn-run-cron"
            onclick="runCron('{{ cronUrl|e('js') }}', this)">
        <i class="material-icons" style="font-size:1rem;vertical-align:middle">play_arrow</i>
        Cron J-1
    </button>
    <button type="button" class="btn btn-outline-primary btn-sm tab-action tab-action-resources" id="btn-sample-now"
            onclick="sampleNow(this)">
        <i class="material-icons" style="font-size:1rem;vertical-align:middle">timer</i>
        Sample now
    </button>
    <button type="button" class="btn btn-outline-secondary btn-sm tab-action tab-action-resources" id="btn-run-cron-resources"
            onclick="runCron('{{ cronResourcesUrl|e('js') }}', this)">
        <i class="material-icons" style="font-size:1rem;vertical-align:middle">memory</i>
        Rollup J-1
    </button>
    <span id="cron-run-result" class="ml-2 small tab-action tab-action-trafic tab-action-conversion tab-action-resources"></span>
    {% endif %}
</div>

{# ------------------------------------------------------------------ #}
{# Configuration form (hidden if already configured)                   #}
{# ------------------------------------------------------------------ #}
<div id="config-section" class="card mb-4" {% if logsPath is not empty %}style="display:none"{% endif %}>
    <div class="card-header">
        <h3 class="card-header-title">
            <i class="material-icons">settings</i>
            Configuration
        </h3>
    </div>
    <div class="card-body">
        {% if logsPath is empty %}
        <div class="alert alert-warning">
            <i class="material-icons align-middle mr-1" style="font-size:1rem">warning</i>
            Configurez le chemin des logs pour commencer.
        </div>
        {% endif %}
        <form method="post" action="{{ path('sc_alwaysdata_index') }}">
            <input type="hidden" name="_token" value="{{ csrf_token('sc_alwaysdata_index') }}">

            <div class="form-group">
                <label for="logs_path">Chemin des logs</label>
                <input
                    type="text"
                    id="logs_path"
                    name="logs_path"
                    class="form-control"
                    value="{{ logsPath }}"
                    placeholder="{{ logsPathHint }}"
                    required
                >
                <small class="form-text text-muted">
                    Chemin absolu vers le répertoire des logs Alwaysdata (ex : {{ logsPathHint }})
                </small>
            </div>

            <div class="form-group">
                <label for="htaccess_path">Chemin .htaccess</label>
                <input
                    type="text"
                    id="htaccess_path"
                    name="htaccess_path"
                    class="form-control"
                    value="{{ htaccessPath }}"
                >
                <small class="form-text text-muted">
                    Chemin absolu vers le fichier .htaccess de PrestaShop.
                </small>
            </div>

            <div class="form-group">
                <label>Lignes lues (tail -n) par source</label>
                <div class="form-row">
                    {% for dir, default in {apache: 5000, http: 10000, php: 5000, sites: 2000} %}
                    <div class="col-md-3">
                        <div class="input-group mb-2">
                            <div class="input-group-prepend">
                                <span class="input-group-text" style="min-width:60px">{{ dir }}</span>
                            </div>
                            <input type="number" name="lines_{{ dir }}" class="form-control"
                                   value="{{ linesPerSource[dir] ?? default }}" min="100" max="100000">
                        </div>
                    </div>
                    {% endfor %}
                </div>
                <small class="form-text text-muted">Nombre de lignes lues depuis la fin de chaque fichier log.</small>
            </div>

            <button type="submit" class="btn btn-primary">
                <i class="material-icons">save</i>
                Sauvegarder
            </button>
        </form>

        {% if cronUrl is not empty %}
        <div class="mt-4 border-top pt-3">
          <h6><i class="material-icons align-middle" style="font-size:1rem">schedule</i> Configuration crons Alwaysdata</h6>
          <p class="text-muted small mb-2">
            Dans le panneau Alwaysdata → <strong>Tâches planifiées</strong>, créer <strong>3 tâches</strong> de type URL :
          </p>

          <div class="card card-body bg-light mb-2 p-2">
            <div class="small font-weight-bold mb-1">1. Cron Stats (logs HTTP J-1)</div>
            <div class="small text-muted mb-1">Fréquence recommandée : <strong>tous les jours à 02:00</strong></div>
            <div class="input-group input-group-sm">
              <input type="text" class="form-control font-monospace" id="cron-url-config" value="{{ cronUrl }}" readonly>
              <div class="input-group-append">
                <button class="btn btn-outline-secondary" type="button"
                        onclick="var i=document.getElementById('cron-url-config');i.select();document.execCommand('copy')">Copier</button>
              </div>
            </div>
          </div>

          <div class="card card-body bg-light mb-2 p-2">
            <div class="small font-weight-bold mb-1">2. Cron Sample Ressources (haute fréquence)</div>
            <div class="small text-muted mb-1">Fréquence recommandée : <strong>toutes les 15 minutes</strong> — collecte RAM / CPU / OPcache / top processes</div>
            <div class="input-group input-group-sm">
              <input type="text" class="form-control font-monospace" id="cron-sample-url-config" value="{{ cronResourcesSampleUrl }}" readonly>
              <div class="input-group-append">
                <button class="btn btn-outline-secondary" type="button"
                        onclick="var i=document.getElementById('cron-sample-url-config');i.select();document.execCommand('copy')">Copier</button>
              </div>
            </div>
          </div>

          <div class="card card-body bg-light mb-2 p-2">
            <div class="small font-weight-bold mb-1">3. Cron Rollup Ressources (agrégation J-1)</div>
            <div class="small text-muted mb-1">Fréquence recommandée : <strong>tous les jours à 02:15</strong> — consolide les samples de la veille en historique long (90j)</div>
            <div class="input-group input-group-sm">
              <input type="text" class="form-control font-monospace" id="cron-resources-url-config" value="{{ cronResourcesUrl }}" readonly>
              <div class="input-group-append">
                <button class="btn btn-outline-secondary" type="button"
                        onclick="var i=document.getElementById('cron-resources-url-config');i.select();document.execCommand('copy')">Copier</button>
              </div>
            </div>
          </div>
        </div>
        {% endif %}

    </div>
</div>

{% if logsPath is empty %}
<div class="alert alert-info">
    <i class="material-icons align-middle mr-1">info</i>
    Renseignez le chemin des logs ci-dessus pour démarrer l'analyse.
</div>
{% endif %}

<input type="hidden" id="csrf-apply-block" value="{{ csrf_token('sc_alwaysdata_apply_block') }}">

{# ------------------------------------------------------------------ #}
{# Main tabs — Statistiques trafic | Conversion | Logs du jour         #}
{# ------------------------------------------------------------------ #}
<div id="main-tabs-section" class="mb-4">
    <div class="d-flex align-items-end flex-wrap mb-3" style="border-bottom:1px solid #dee2e6">
        <ul class="nav nav-tabs flex-grow-1" id="main-nav-tabs" role="tablist" style="border-bottom:0;margin-bottom:-1px">
            {% if logsPath is not empty %}
            <li class="nav-item">
                <a class="nav-link active" id="tab-logs-link" data-toggle="tab" href="#tab-logs" role="tab">
                    <i class="material-icons align-middle" style="font-size:1rem">list_alt</i>
                    Logs du jour
                </a>
            </li>
            {% endif %}
            <li class="nav-item">
                <a class="nav-link {% if logsPath is empty %}active{% endif %}" id="tab-trafic-link" data-toggle="tab" href="#tab-trafic" role="tab">
                    <i class="material-icons align-middle" style="font-size:1rem">bar_chart</i>
                    Statistiques trafic
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" id="tab-conversion-link" data-toggle="tab" href="#tab-conversion" role="tab">
                    <i class="material-icons align-middle" style="font-size:1rem">trending_up</i>
                    Conversion
                </a>
            </li>
            <li class="nav-item">
                <a class="nav-link" id="tab-resources-link" data-toggle="tab" href="#tab-resources" role="tab">
                    <i class="material-icons align-middle" style="font-size:1rem">memory</i>
                    Ressources
                </a>
            </li>
        </ul>
        <div class="d-flex align-items-center d-none pb-2" id="stats-controls" style="gap:.5rem">
            <input type="date" id="stats-date-picker" class="form-control form-control-sm" style="width:auto">
            <div class="btn-group btn-group-sm" role="group">
                <button type="button" class="btn btn-outline-secondary stats-period" data-period="30">30j</button>
                <button type="button" class="btn btn-outline-secondary stats-period active" data-period="7">7j</button>
            </div>
            <div id="stats-spinner" style="display:none">
                <div class="spinner-border spinner-border-sm text-primary" role="status"></div>
            </div>
        </div>
    </div>
    <div class="tab-content" id="main-tab-content">

        {# --- Tab Trafic --- #}
        {{ include('@Modules/sc_alwaysdata/views/templates/admin/_tab_trafic.html.twig', {'isActiveTab': logsPath is empty}) }}

        {# --- Tab Conversion --- #}
        {{ include('@Modules/sc_alwaysdata/views/templates/admin/_tab_conversion.html.twig') }}

        {# --- Tab Ressources --- #}
        {{ include('@Modules/sc_alwaysdata/views/templates/admin/_tab_resources.html.twig') }}

        {# --- Tab Logs du jour --- #}
        {% if logsPath is not empty %}
        <div class="tab-pane fade show active" id="tab-logs" role="tabpanel">
            {# Source status badges #}
            <div id="sources-status" class="pt-3"></div>

            {# Log viewer — tabs built by JS #}
            <div id="logs-viewer-section">
                <div class="pt-2">
                    <ul class="nav nav-tabs" id="logs-tabs" role="tablist"></ul>
                </div>
                <div class="tab-content" id="logs-tab-content"></div>
            </div>
        </div>
        {% endif %}

    </div>
</div>

{# ------------------------------------------------------------------ #}
{# Inline JavaScript                                                    #}
{# ------------------------------------------------------------------ #}
<script>
var analyseUrl = '{{ path('sc_alwaysdata_analyse') }}';
var applyBlockUrl = '{{ path('sc_alwaysdata_apply_block') }}';
var applyUnblockUrl = '{{ path('sc_alwaysdata_apply_unblock') }}';
var statsUrl = '{{ statsUrl }}';
var conversionUrl = '{{ conversionUrl }}';
var resourcesStatsUrl = '{{ resourcesStatsUrl }}';
var cronResourcesUrl = '{{ cronResourcesUrl }}';
var cronResourcesSampleUrl = '{{ cronResourcesSampleUrl }}';
window.cronResourcesSampleUrl = cronResourcesSampleUrl;
</script>

<script>
// Helpers globaux utilisés par les partiels _tab_trafic et _tab_conversion
function esc(s) {
    return String(s).replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');
}
function pct(a, b) {
    if (!b || b === 0) return '-';
    return Math.round(a / b * 100) + '%';
}
</script>

<script>
(function() {
    var yesterday = (function() {
        var d = new Date(); d.setDate(d.getDate() - 1);
        var mm = String(d.getMonth() + 1).padStart(2, '0');
        var dd = String(d.getDate()).padStart(2, '0');
        return d.getFullYear() + '-' + mm + '-' + dd;
    })();

    var today = (function() {
        var d = new Date();
        var mm = String(d.getMonth() + 1).padStart(2, '0');
        var dd = String(d.getDate()).padStart(2, '0');
        return d.getFullYear() + '-' + mm + '-' + dd;
    })();

    var currentPeriod = 7;
    var currentDate = yesterday;
    var activeTab = 'trafic';

    var datePicker = document.getElementById('stats-date-picker');
    var spinner = document.getElementById('stats-spinner');
    if (datePicker) { datePicker.value = yesterday; datePicker.max = yesterday; }

    function updateTabActions(tabName) {
        document.querySelectorAll('.tab-action').forEach(function(el) {
            var show = el.classList.contains('tab-action-' + tabName);
            el.style.display = show ? '' : 'none';
        });
    }
    window.updateTabActions = updateTabActions;

    function postJson(url, params) {
        var body = new URLSearchParams(params);
        return fetch(url, {
            method: 'POST',
            headers: { 'Content-Type': 'application/x-www-form-urlencoded', 'X-Requested-With': 'XMLHttpRequest' },
            body: body.toString()
        });
    }

    function fetchStats() {
        if (spinner) spinner.style.display = 'inline-block';
        postJson(statsUrl, { date: currentDate, period: String(currentPeriod) })
            .then(function(r) { return r.json(); })
            .then(function(d) { renderTrafic(d, currentDate, currentPeriod); })
            .catch(function(e) { document.getElementById('tab-trafic-content').innerHTML = '<div class="alert alert-danger">Erreur: ' + esc(e.message) + '</div>'; })
            .finally(function() { if (spinner) spinner.style.display = 'none'; });
    }

    function fetchConversion() {
        if (spinner) spinner.style.display = 'inline-block';
        postJson(conversionUrl, { date: currentDate, period: String(currentPeriod) })
            .then(function(r) { return r.json(); })
            .then(function(d) { renderConversion(d, currentDate, currentPeriod); })
            .catch(function(e) { document.getElementById('tab-conversion-content').innerHTML = '<div class="alert alert-danger">Erreur: ' + esc(e.message) + '</div>'; })
            .finally(function() { if (spinner) spinner.style.display = 'none'; });
    }

    function fetchResources() {
        if (spinner) spinner.style.display = 'inline-block';
        postJson(resourcesStatsUrl, { date: currentDate, period: String(currentPeriod) })
            .then(function(r) { return r.json(); })
            .then(function(d) { renderResources(d, currentDate, currentPeriod); })
            .catch(function(e) { document.getElementById('tab-resources-content').innerHTML = '<div class="alert alert-danger">Erreur: ' + esc(e.message) + '</div>'; })
            .finally(function() { if (spinner) spinner.style.display = 'none'; });
    }

    function fetchActive() {
        if (activeTab === 'trafic') fetchStats();
        else if (activeTab === 'conversion') fetchConversion();
        else if (activeTab === 'resources') fetchResources();
    }

    // Exposés globalement pour runCron() dans _tab_trafic.html.twig
    window.fetchStats = fetchStats;
    window.fetchResources = fetchResources;

    document.addEventListener('DOMContentLoaded', function() {
        // Date picker
        if (datePicker) {
            datePicker.addEventListener('change', function() {
                currentDate = this.value || yesterday;
                fetchActive();
            });
        }

        // Period buttons
        document.querySelectorAll('.stats-period').forEach(function(btn) {
            btn.addEventListener('click', function() {
                document.querySelectorAll('.stats-period').forEach(function(b) { b.classList.remove('active'); });
                this.classList.add('active');
                currentPeriod = parseInt(this.dataset.period, 10);
                fetchActive();
            });
        });

        // Tab switch — Bootstrap 4 utilise jQuery, il faut .on() et non addEventListener
        $('#main-nav-tabs a[data-toggle="tab"]').on('shown.bs.tab', function(e) {
            var tabId = $(e.target).attr('id');
            var statsControls = document.getElementById('stats-controls');
            var tabName = tabIdToName(tabId);

            // Persist active tab in URL hash (sans scroll jump grâce à replaceState)
            if (tabName) {
                history.replaceState(null, '', '#' + tabName);
            }

            if (tabId === 'tab-logs-link') {
                if (statsControls) statsControls.classList.add('d-none');
                updateTabActions('logs');
                if (!window.analysisRan) {
                    window.runAnalysis && window.runAnalysis();
                }
            } else {
                if (statsControls) statsControls.classList.remove('d-none');
                if (tabId === 'tab-trafic-link') {
                    activeTab = 'trafic';
                    // Ressources autorise aujourd'hui — rétablir hier pour les autres onglets
                    if (datePicker) { datePicker.max = yesterday; if (currentDate > yesterday) { currentDate = yesterday; datePicker.value = yesterday; } }
                } else if (tabId === 'tab-conversion-link') {
                    activeTab = 'conversion';
                    if (datePicker) { datePicker.max = yesterday; if (currentDate > yesterday) { currentDate = yesterday; datePicker.value = yesterday; } }
                } else if (tabId === 'tab-resources-link') {
                    activeTab = 'resources';
                    // Ressources : autoriser aujourd'hui
                    if (datePicker) { datePicker.max = today; if (currentDate === yesterday) { currentDate = today; datePicker.value = today; } }
                }
                updateTabActions(activeTab);
                fetchActive();
            }
        });

        function tabIdToName(tabId) {
            if (tabId === 'tab-logs-link')       return 'logs';
            if (tabId === 'tab-trafic-link')     return 'trafic';
            if (tabId === 'tab-conversion-link') return 'conversion';
            if (tabId === 'tab-resources-link')  return 'resources';
            return null;
        }

        // --- Initial tab selection ---
        // Priorité : hash URL > onglet .active dans le DOM (défini côté template)
        var hashTab = (window.location.hash || '').replace(/^#/, '');
        var validTabs = ['logs', 'trafic', 'conversion', 'resources'];
        var hashTarget = validTabs.indexOf(hashTab) !== -1
            ? document.getElementById('tab-' + hashTab + '-link')
            : null;

        if (hashTarget) {
            // Basculer vers l'onglet demandé — shown.bs.tab se chargera du reste (fetchActive etc.)
            $(hashTarget).tab('show');
        } else {
            // Défaut : lire l'onglet .active posé par le template, initialiser en conséquence
            var initialActiveTabLink = document.querySelector('#main-nav-tabs .nav-link.active');
            var initialTabName = 'trafic';
            if (initialActiveTabLink) {
                initialTabName = tabIdToName(initialActiveTabLink.id) || 'trafic';
            }
            updateTabActions(initialTabName);
            if (initialTabName !== 'logs' && statsControls) {
                statsControls.classList.remove('d-none');
            }
            if (initialTabName === 'trafic')          fetchStats();
            else if (initialTabName === 'conversion') { activeTab = 'conversion'; fetchConversion(); }
            else if (initialTabName === 'resources')  { activeTab = 'resources';  fetchResources(); }
            // logs : pas de fetch, runAnalysis tourne déjà via son propre script
        }
    });
})();
</script>

<script>
document.addEventListener('DOMContentLoaded', function() {
    var btn = document.getElementById('btn-analyse');
    var spinner = document.getElementById('analyse-spinner');
    var configSection = document.getElementById('config-section');
    var btnToggleConfig = document.getElementById('btn-toggle-config');

    // Flag pour savoir si l'analyse a déjà été lancée
    window.analysisRan = false;

    // Toggle config form
    btnToggleConfig.addEventListener('click', function() {
        var hidden = configSection.style.display === 'none';
        configSection.style.display = hidden ? 'block' : 'none';
    });

    if (!btn) return;

    function runAnalysis() {
        btn.disabled = true;
        spinner.style.display = 'inline-flex';

        fetch(analyseUrl, {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
                'X-Requested-With': 'XMLHttpRequest'
            },
            body: JSON.stringify({_token: getCsrfToken()})
        })
        .then(function(r) {
            var contentType = r.headers.get('content-type') || '';
            if (!contentType.includes('application/json')) {
                return r.text().then(function(html) {
                    throw new Error('Le serveur a renvoyé du HTML (status ' + r.status + '). Vérifiez les logs serveur ou videz le cache Symfony.');
                });
            }
            return r.json();
        })
        .then(function(data) {
            window.analysisRan = true;
            renderResults(data);
        })
        .catch(function(err) { showError(err.message); })
        .finally(function() {
            btn.disabled = false;
            spinner.style.display = 'none';
        });
    }

    // Exposé globalement pour le tab switch
    window.runAnalysis = runAnalysis;

    btn.addEventListener('click', function() {
        // Forcer re-run et switcher sur l'onglet Logs
        window.analysisRan = false;
        var logsTabLink = document.getElementById('tab-logs-link');
        if (logsTabLink) {
            $(logsTabLink).tab('show');
        } else {
            runAnalysis();
        }
    });

    // Auto-run on page load
    runAnalysis();

    function renderResults(data) {
        if (data.error) {
            showError(data.error);
            return;
        }
        renderSources(data.sources || []);
        renderLogViewer(data.sources || [], data.crawlers || [], data.php_errors || [], data.blocked_ips || [], data.blocked_uas || []);
    }

    function renderSources(sources) {
        var container = document.getElementById('sources-status');
        if (!container) return;
        if (!sources.length) { container.innerHTML = ''; return; }
        container.innerHTML = sources.map(function(s) {
            var cls = 'success';
            var label = escapeHtml(s.source);
            if (s.error) {
                cls = 'danger';
                label += ': ' + escapeHtml(s.error);
            } else if (s.truncated) {
                cls = 'warning';
                label += ' (tronqu\u00e9)';
            }
            return '<span class="badge badge-' + cls + ' mr-1">' + label + '</span>';
        }).join('');
    }

    function renderLogViewer(sources, crawlers, phpErrors, blockedIps, blockedUas) {
        var tabs = document.getElementById('logs-tabs');
        var content = document.getElementById('logs-tab-content');
        if (!tabs || !content) return;

        tabs.innerHTML = '';
        content.innerHTML = '';

        var savedTab = localStorage.getItem('sc_alwaysdata_tab') || '';

        sources.forEach(function(s, i) {
            var id = 'log-tab-' + s.source;
            var isActive = savedTab ? (id === savedTab) : (i === 0);
            var active = isActive ? ' active' : '';
            var show = isActive ? ' show active' : '';

            var label = s.source;
            if (s.error) {
                label += ' <span class="badge badge-danger ml-1">erreur</span>';
            } else {
                label += ' <span class="badge badge-secondary ml-1">' + (s.line_count || 0) + ' lignes</span>';
            }

            tabs.innerHTML += '<li class="nav-item">'
                + '<a class="nav-link' + active + '" data-toggle="tab" href="#' + id + '" data-tab-id="' + id + '">' + label + '</a>'
                + '</li>';

            var analysisBlock = '';
            if (s.source === 'http') {
                analysisBlock = buildCrawlersBlock(crawlers || [], blockedIps || [], blockedUas || []);
            } else if (s.source === 'php') {
                analysisBlock = buildPhpErrorsBlock(phpErrors || []);
            }

            var logBlock;
            if (s.error) {
                logBlock = '<div class="alert alert-danger m-3">' + escapeHtml(s.error) + '</div>';
            } else if (!s.preview || !s.preview.length) {
                logBlock = '<div class="alert alert-info m-3">Aucune ligne disponible.</div>';
            } else {
                var lines = s.preview.map(function(l) { return escapeHtml(l); }).join('\n');
                logBlock = '<pre class="m-0 p-3" style="font-size:0.75rem;max-height:400px;overflow-y:auto;background:#1e1e1e;color:#d4d4d4;white-space:pre-wrap;word-break:break-all;">'
                    + lines + '</pre>';
            }

            content.innerHTML += '<div class="tab-pane fade' + show + '" id="' + id + '">'
                + analysisBlock + logBlock + '</div>';
        });

        tabs.querySelectorAll('a[data-toggle="tab"]').forEach(function(a) {
            a.addEventListener('shown.bs.tab', function() {
                localStorage.setItem('sc_alwaysdata_tab', this.dataset.tabId);
            });
        });

        attachBlockHandlers();
        attachUnblockHandlers();
    }

    function buildCrawlersBlock(crawlers, blockedIps, blockedUas) {
        // Crawlers detected in today's logs, not yet blocked
        var active = crawlers.filter(function(c) { return !c.already_blocked; });

        // Index crawlers-from-log by IP for quick lookup (to enrich the blocked section)
        var crawlerByIp = {};
        crawlers.forEach(function(c) {
            (c.ips || []).forEach(function(ip) { crawlerByIp[ip] = c; });
        });

        var theadDetected = '<thead class="thead-light"><tr>'
            + '<th>Bot / IP</th><th>User-Agents</th><th>Requ\u00eates/jour</th><th>Raison</th><th>Action</th>'
            + '</tr></thead>';

        // --- Section 1 : crawlers détectés aujourd'hui (non bloqués) ---
        var activeRows = active.length
            ? active.map(function(c) {
                var isKnown = c.reason === 'known_bot';
                var ipList = c.ips || [];

                // Bot/IP column
                var ipCell;
                if (isKnown) {
                    ipCell = '<strong>' + escapeHtml(c.matched_bot || '') + '</strong>'
                        + (ipList.length ? '<br><small class="text-muted">' + ipList.length + '\u00a0IP(s)</small>' : '');
                } else {
                    ipCell = '<code>' + escapeHtml(ipList[0] || '?') + '</code>';
                }

                // UA column
                var uaList = c.user_agents || [];
                var uaCell = uaList.length
                    ? '<small style="word-break:break-all">' + escapeHtml(uaList[0]) + '</small>'
                      + (uaList.length > 1 ? ' <span class="badge badge-light">+' + (uaList.length - 1) + '</span>' : '')
                    : '<em class="text-muted">-</em>';

                // Actions
                var actions;
                if (isKnown) {
                    actions = '<button class="btn btn-sm btn-danger apply-block" data-type="ua" data-value="' + escapeAttr(c.matched_bot || '') + '">'
                        + 'Bloquer ' + escapeHtml(c.matched_bot || '') + '</button>';
                } else {
                    actions = '<button class="btn btn-sm btn-danger apply-block" data-type="ip" data-value="' + escapeAttr(ipList[0] || '') + '">'
                        + 'Bloquer l\'IP</button>';
                }

                var reasonBadge = '<span class="badge badge-' + (isKnown ? 'warning' : 'info') + '">'
                    + (isKnown ? 'Bot connu' : 'Haute fr\u00e9quence') + '</span>';

                return '<tr>'
                    + '<td style="min-width:140px">' + ipCell + '</td>'
                    + '<td>' + uaCell + '</td>'
                    + '<td class="text-right">' + escapeHtml(String(c.request_count)) + '</td>'
                    + '<td>' + reasonBadge + '</td>'
                    + '<td style="white-space:nowrap">' + actions + '</td>'
                    + '</tr>';
            }).join('')
            : '<tr><td colspan="5" class="text-center text-muted">Aucun nouveau crawler invasif d\u00e9tect\u00e9</td></tr>';

        var html = '<div class="p-3 border-bottom">'
            + '<h6 class="mb-2"><i class="material-icons align-middle" style="font-size:1rem">bug_report</i>'
            + ' Crawlers d\u00e9tect\u00e9s aujourd\'hui'
            + (active.length ? ' <span class="badge badge-danger ml-1">' + active.length + '</span>' : '')
            + '</h6>'
            + '<table class="table table-sm mb-0">' + theadDetected + '<tbody>' + activeRows + '</tbody></table>'
            + '</div>';

        // --- Section 2 : règles .htaccess existantes (avec bouton Débloquer) ---
        var hasBlocked = blockedIps.length > 0 || blockedUas.length > 0;
        var theadBlocked = '<thead class="thead-light"><tr><th>Type</th><th>Valeur</th><th>Requ\u00eates aujourd\'hui</th><th>Action</th></tr></thead>';

        var blockedRows = '';

        blockedIps.forEach(function(ip) {
            var entry = crawlerByIp[ip];
            var count = entry ? entry.request_count : '-';
            blockedRows += '<tr data-blocked-type="ip" data-blocked-value="' + escapeAttr(ip) + '">'
                + '<td><span class="badge badge-secondary">IP</span></td>'
                + '<td><code>' + escapeHtml(ip) + '</code></td>'
                + '<td>' + escapeHtml(String(count)) + '</td>'
                + '<td><button class="btn btn-sm btn-outline-warning apply-unblock" data-type="ip" data-value="' + escapeAttr(ip) + '">D\u00e9bloquer</button></td>'
                + '</tr>';
        });

        blockedUas.forEach(function(ua) {
            blockedRows += '<tr data-blocked-type="ua" data-blocked-value="' + escapeAttr(ua) + '">'
                + '<td><span class="badge badge-secondary">User-Agent</span></td>'
                + '<td><small>' + escapeHtml(ua) + '</small></td>'
                + '<td>-</td>'
                + '<td><button class="btn btn-sm btn-outline-warning apply-unblock" data-type="ua" data-value="' + escapeAttr(ua) + '">D\u00e9bloquer</button></td>'
                + '</tr>';
        });

        if (!hasBlocked) {
            blockedRows = '<tr><td colspan="4" class="text-center text-muted">Aucune r\u00e8gle de blocage dans .htaccess</td></tr>';
        }

        html += '<div class="p-3 border-bottom">'
            + '<h6 class="mb-2"><i class="material-icons align-middle" style="font-size:1rem">block</i>'
            + ' D\u00e9j\u00e0 bloqu\u00e9s dans .htaccess'
            + (hasBlocked ? ' <span class="badge badge-secondary ml-1">' + (blockedIps.length + blockedUas.length) + '</span>' : '')
            + '</h6>'
            + '<table class="table table-sm mb-0">' + theadBlocked + '<tbody>' + blockedRows + '</tbody></table>'
            + '</div>';

        return html;
    }

    function buildPhpErrorsBlock(errors) {
        var cls = {fatal:'danger',error:'danger',warning:'warning',notice:'secondary',deprecated:'light'};
        var rows = !errors.length
            ? '<tr><td colspan="5" class="text-center text-muted">Aucune erreur PHP d\u00e9tect\u00e9e</td></tr>'
            : errors.map(function(e) {
                var c = cls[e.level] || 'secondary';
                var f = String(e.file || '');
                f = f.length > 60 ? '...' + f.slice(-60) : f;
                return '<tr><td><span class="badge badge-' + c + '">' + escapeHtml(e.level) + '</span></td>'
                    + '<td><small>' + escapeHtml(f) + '</small></td>'
                    + '<td>' + escapeHtml(String(e.line || '-')) + '</td>'
                    + '<td><small>' + escapeHtml(e.message || '') + '</small></td>'
                    + '<td>' + escapeHtml(String(e.count || 0)) + '</td></tr>';
            }).join('');
        return '<div class="p-3 border-bottom">'
            + '<h6 class="mb-2"><i class="material-icons align-middle" style="font-size:1rem">error_outline</i> Erreurs PHP</h6>'
            + '<table class="table table-sm mb-0"><thead class="thead-light"><tr>'
            + '<th>Niveau</th><th>Fichier</th><th>Ligne</th><th>Message</th><th>Occurrences</th>'
            + '</tr></thead><tbody>' + rows + '</tbody></table></div>';
    }

    function attachUnblockHandlers() {
        document.querySelectorAll('.apply-unblock').forEach(function(btn) {
            btn.addEventListener('click', function() {
                var type = this.dataset.type;
                var value = this.dataset.value;
                var row = this.closest('tr');
                var self = this;
                self.disabled = true;

                var formData = new URLSearchParams();
                formData.append('_token', getCsrfToken());
                formData.append('type', type);
                formData.append('value', value);

                fetch(applyUnblockUrl, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/x-www-form-urlencoded',
                        'X-Requested-With': 'XMLHttpRequest'
                    },
                    body: formData.toString()
                })
                .then(function(r) { return r.json(); })
                .then(function(data) {
                    if (data.success) {
                        row.style.opacity = '0.4';
                        row.querySelector('td:last-child').innerHTML = '<span class="badge badge-light">D\u00e9bloqu\u00e9</span>';
                    } else {
                        alert('Erreur: ' + (data.message || 'Erreur inconnue'));
                        self.disabled = false;
                    }
                })
                .catch(function() {
                    self.disabled = false;
                });
            });
        });
    }

    function attachBlockHandlers() {
        document.querySelectorAll('.apply-block').forEach(function(blockBtn) {
            blockBtn.addEventListener('click', function() {
                var type = this.dataset.type;
                var value = this.dataset.value;
                var row = this.closest('tr');
                var self = this;
                self.disabled = true;

                var formData = new URLSearchParams();
                formData.append('_token', getCsrfToken());
                formData.append('type', type);
                formData.append('value', value);

                fetch(applyBlockUrl, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/x-www-form-urlencoded',
                        'X-Requested-With': 'XMLHttpRequest'
                    },
                    body: formData.toString()
                })
                .then(function(r) { return r.json(); })
                .then(function(data) {
                    if (data.success) {
                        row.querySelector('td:last-child').innerHTML = '<span class="badge badge-secondary">D\u00e9j\u00e0 bloqu\u00e9</span>';
                        row.classList.add('text-muted');
                    } else {
                        alert('Erreur: ' + (data.message || 'Erreur inconnue'));
                        self.disabled = false;
                    }
                })
                .catch(function() {
                    self.disabled = false;
                });
            });
        });
    }

    function getCsrfToken() {
        var input = document.getElementById('csrf-apply-block');
        return input ? input.value : '';
    }

    function escapeHtml(str) {
        return String(str)
            .replace(/&/g, '&amp;')
            .replace(/</g, '&lt;')
            .replace(/>/g, '&gt;')
            .replace(/"/g, '&quot;')
            .replace(/'/g, '&#039;');
    }

    function escapeAttr(str) {
        return String(str)
            .replace(/&/g, '&amp;')
            .replace(/"/g, '&quot;')
            .replace(/'/g, '&#039;');
    }

    function showError(msg) {
        var sourcesStatus = document.getElementById('sources-status');
        if (sourcesStatus) {
            sourcesStatus.innerHTML = '<div class="alert alert-danger m-0">'
                + '<i class="material-icons align-middle mr-1">error</i>'
                + escapeHtml(msg)
                + '</div>';
        }
    }
});
</script>

{% endblock %}
